name: Auto Checker Cron

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes
  workflow_dispatch:        # Manual trigger in GitHub UI

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call Railway cron tick endpoint
        run: |
          # Random sleep 0-60 seconds to spread out checks
          sleep $((RANDOM % 60))
          curl -X GET "${{ secrets.RAILWAY_APP_URL }}/api/cron/tick" \
            -H "x-cron-secret: ${{ secrets.CRON_SECRET }}"
